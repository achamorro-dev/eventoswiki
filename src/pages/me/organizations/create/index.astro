---
import Layout from '@/layouts/layout.astro'
import CreateOrganizationPage from '@/organizations/presentation/client/pages/create-organization-page/create-organization-page.astro'
import { Urls } from '@/ui/urls/urls'

const userId = Astro.locals.user?.id

if (!userId) {
  return Astro.redirect(Urls.LOGIN)
}
---

<Layout title="Crear organización">
  <CreateOrganizationPage />
</Layout>
