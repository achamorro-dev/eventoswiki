---
import { Urls } from '@/ui/urls/urls'

const { handle } = Astro.params

if (!handle) {
  Astro.response.status = 404
  Astro.response.statusText = 'Not found'
  return
}

return Astro.redirect(Urls.ORGANIZATION_EVENTS(handle))
---
