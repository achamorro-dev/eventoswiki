<article class="content"><slot /></article>

<style is:global>
  @reference "../../../../../../styles/global.css";

  .content {
    @apply mx-auto max-w-3xl;

    body {
      @apply bg-background text-foreground dark:bg-background dark:text-foreground m-0 grid min-h-full grid-rows-[auto_auto_1fr_auto] scroll-smooth;
    }

    p,
    span {
      @apply text-lg text-gray-900 dark:text-gray-50;
    }

    small {
      @apply text-sm text-gray-900 dark:text-gray-50;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      @apply !font-bold mb-0;
    }

    h1 {
      @apply text-primary dark:text-foreground text-3xl;
    }

    h2 {
      @apply text-foreground dark:text-foreground py-4 !text-3xl;
    }

    h3 {
      @apply text-foreground dark:text-foreground py-3 !text-2xl;
    }

    p {
      @apply py-2;
    }

    table {
      @apply mb-10;
    }

    table tr {
      @apply border-b dark:border-b-gray-50;
    }

    table td {
      @apply p-4 text-gray-900 dark:text-gray-50;
    }

    table th {
      @apply p-4 !text-gray-900 dark:!text-gray-50;
    }

    ol,
    ul {
      @apply p-1 font-sans text-base;
    }

    ol {
      @apply list-decimal;
    }

    ul {
      @apply list-disc;
    }

    ul li {
      @apply text-foreground dark:text-foreground mb-2 !text-lg;
    }

    ol li {
      @apply text-foreground dark:text-foreground mb-2 !text-lg;
    }

    a {
      @apply underline;
    }
  }
</style>
