---
import type { ClassesComponent } from '@/ui/classnames/classes-component'
import BaseInput from '../base-input/base-input.astro'
import type { Input } from '../types/input'

interface Props extends Input, ClassesComponent {}

const { label, classes, variant = 'default', error, ...rest } = Astro.props
---

<BaseInput label={label} error={error}>
  <input type="text" class:list={['input', `input-${variant}`, { 'input-with-error': error }, classes]} {...rest} />
</BaseInput>
