---
interface Props {
  contentId?: string
}

const { contentId = 'main' } = Astro.props
---

<a
  class="absolute -left-full -top-full z-50 border border-primary bg-background px-4 py-2 text-primary focus:left-0 focus:top-0 dark:bg-background dark:text-primary dark:focus:text-gray-200"
  href={`#${contentId}`}
  tabindex="0">Ir al contenido</a
>
