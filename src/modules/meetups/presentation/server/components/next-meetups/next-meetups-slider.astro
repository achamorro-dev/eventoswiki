---
import EventsSlider from '@/ui/components/events-slider/events-slider.astro'
import { Urls } from '@/ui/urls/urls'
import { MeetupsLocator } from '../../../../di/meetups.locator'

interface Props {
  location?: string
}

const { location } = Astro.props
const { data: nextMeetups } = await MeetupsLocator.getNextMeetupsQuery().execute({ count: 10, location })
---

<EventsSlider
  title={'Próximos meetups' + (location ? ` en ${location}` : '')}
  seeMoreLink={Urls.MEETUPS}
  goToEvent={Urls.MEETUP}
  events={nextMeetups}
/>
