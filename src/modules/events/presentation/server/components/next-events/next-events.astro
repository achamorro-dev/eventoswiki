---
import { EventsLocator } from '@/events/di/events.locator'
import EventsSlider from '@/ui/components/events-slider/events-slider.astro'
import { Urls } from '@/ui/urls/urls'

interface Props {
  location?: string
}

const { location } = Astro.props
const { data: nextEvents } = await EventsLocator.getNextEventsQuery().execute({ count: 10, location })
---

<EventsSlider
  title={'Próximos eventos' + (location ? ` en ${location}` : '')}
  seeMoreLink={Urls.EVENTS}
  goToEvent={Urls.EVENT}
  events={nextEvents}
/>
